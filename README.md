user data

```
#!/bin/bash
sudo apt-get update
sudo apt-get install git
curl -sL https://deb.nodesource.com/setup_14.x | sudo bash -
sudo apt -y install nodejs
git clone https://github.com/Manuel11713/test-node-aws-course.git
sudo npm install pm2 -g
cd test-node-aws-course/
npm i
```
